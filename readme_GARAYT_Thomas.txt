Question sur les FindByX :

Les méthodes findBy ou findOneBy de Doctrine se basent sur la méthode "magique" de php __call() qui permet de récupérer l'attribut d'un objet PHP (qui est donné dans le nom de la méthode).
